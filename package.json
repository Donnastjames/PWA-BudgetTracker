{
  "name": "budget-tracker",
  "version": "1.0.0",
  "description": "Budget Tracker application",
  "scripts": {
    "postinstall": "npm install --prefix ./Develop",
    "build": "npm build --prefix ./Develop",
    "start": "node server.js",
    "lite-server": "lite-server --prefix ./Develop"
  }
}
